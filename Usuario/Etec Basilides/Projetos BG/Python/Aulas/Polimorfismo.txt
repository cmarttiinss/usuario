==========================================
🎭 POLIMORFISMO
==========================================

📝 RESUMO RÁPIDO:
=================

Polimorfismo = "Muitas Formas"
Capacidade de um método responder diferente dependendo do contexto

---

1️⃣ ANALOGIA
===========

Imagine um comando "fazer_som()":

Animal.fazer_som() ?

  🐕 Cachorro → "Au au!"
  🐱 Gato → "Miau!"
  🦅 Pássaro → "Piu piu!"

Mesmo método, respostas diferentes = POLIMORFISMO

---

2️⃣ TIPOS DE POLIMORFISMO
========================

🔴 SOBRESCRITA (Override):
  Classe filha redefine método da classe pai

  class Animal:
      def fazer_som(self):
          return "Som"
  
  class Cachorro(Animal):
      def fazer_som(self):  # Sobrescreve
          return "Au au!"

---

🟠 SOBRECARGA (Overload):
  Python NÃO suporta nativamente
  Mesma função, parâmetros diferentes

  ❌ NÃO FUNCIONA:
    def somar(self, a, b):
        return a + b
    
    def somar(self, a, b, c):  # Sobrescreve anterior
        return a + b + c

  ✅ SOLUÇÃO (argumentos opcionais):
    def somar(self, a, b, c=0):
        return a + b + c

  ✅ SOLUÇÃO (*args):
    def somar(self, *numeros):
        return sum(numeros)

---

🟡 DUCK TYPING:
  "Se parece, nada e grasna como pato... é um pato!"
  
  Tipo não importa, comportamento importa!

  class Pato:
      def fazer_som(self):
          return "Quack!"
  
  class Pessoa:
      def fazer_som(self):
          return "Olá!"
  
  class Máquina:
      def fazer_som(self):
          return "Beeep!"
  
  def fazer_barulho(ser):
      print(ser.fazer_som())
  
  fazer_barulho(Pato())      # Quack! ✓
  fazer_barulho(Pessoa())    # Olá! ✓
  fazer_barulho(Máquina())   # Beeep! ✓

  Sem herança explícita! Flexibilidade máxima.

---

🟢 POLIMORFISMO PARAMÉTRICO:
  Mesmo código funciona com tipos diferentes

  def imprimir_lista(lista):
      for item in lista:
          print(item)
  
  imprimir_lista([1, 2, 3])           # Inteiros
  imprimir_lista(["a", "b", "c"])     # Strings
  imprimir_lista([Obj1(), Obj2()])    # Objetos

---

3️⃣ EXEMPLO 1: ANIMAIS
====================

class Animal:
    def __init__(self, nome):
        self.nome = nome
    
    def fazer_som(self):
        return "Som genérico"
    
    def mover(self):
        return "Se movendo"

class Cachorro(Animal):
    def fazer_som(self):
        return f"{self.nome}: Au au!"
    
    def mover(self):
        return f"{self.nome} está correndo"

class Gato(Animal):
    def fazer_som(self):
        return f"{self.nome}: Miau!"
    
    def mover(self):
        return f"{self.nome} está pulando"

class Passaro(Animal):
    def fazer_som(self):
        return f"{self.nome}: Piu piu!"
    
    def mover(self):
        return f"{self.nome} está voando"

---

POLIMORFISMO EM AÇÃO:

animais = [
    Cachorro("Rex"),
    Gato("Whiskers"),
    Passaro("Tweety")
]

# Mesmo método, respostas diferentes!
for animal in animais:
    print(animal.fazer_som())
    print(animal.mover())

Saída:
  Rex: Au au!
  Rex está correndo
  Whiskers: Miau!
  Whiskers está pulando
  Tweety: Piu piu!
  Tweety está voando

---

4️⃣ EXEMPLO 2: FORMAS GEOMÉTRICAS
=================================

import math

class Forma:
    def calcular_area(self):
        pass

class Quadrado(Forma):
    def __init__(self, lado):
        self.lado = lado
    
    def calcular_area(self):
        return self.lado ** 2

class Circulo(Forma):
    def __init__(self, raio):
        self.raio = raio
    
    def calcular_area(self):
        return math.pi * (self.raio ** 2)

class Triangulo(Forma):
    def __init__(self, base, altura):
        self.base = base
        self.altura = altura
    
    def calcular_area(self):
        return (self.base * self.altura) / 2

---

POLIMORFISMO:

formas = [
    Quadrado(5),
    Circulo(3),
    Triangulo(4, 6)
]

# Mesmo método, contextos diferentes
for forma in formas:
    print(f"Área: {forma.calcular_area():.2f}")

---

5️⃣ EXEMPLO 3: SISTEMAS DE NOTIFICAÇÃO
======================================

class Notificador:
    def enviar(self, msg):
        pass

class Email(Notificador):
    def enviar(self, msg):
        return f"📧 Email: {msg}"

class SMS(Notificador):
    def enviar(self, msg):
        return f"📱 SMS: {msg}"

class Telegram(Notificador):
    def enviar(self, msg):
        return f"✈️ Telegram: {msg}"

---

SISTEMA GENÉRICO:

class SistemaNotificacoes:
    def __init__(self):
        self.notificadores = []
    
    def adicionar(self, notificador):
        self.notificadores.append(notificador)
    
    def notificar_todos(self, msg):
        for notif in self.notificadores:
            print(notif.enviar(msg))

---

USO:

sistema = SistemaNotificacoes()
sistema.adicionar(Email())
sistema.adicionar(SMS())
sistema.adicionar(Telegram())

sistema.notificar_todos("Pedido confirmado!")

Saída:
  📧 Email: Pedido confirmado!
  📱 SMS: Pedido confirmado!
  ✈️ Telegram: Pedido confirmado!

---

6️⃣ VANTAGENS DO POLIMORFISMO
============================

✓ Código flexível e extensível
✓ Adicionar novos tipos é fácil
✓ Sem mudar código existente
✓ Reutilização máxima
✓ Arquitetura limpa

---

7️⃣ BOAS PRÁTICAS
=================

✓ Use nomes consistentes
✓ Documente comportamentos
✓ Mantenha assinatura igual
✓ Teste cada implementação
✓ Duck typing quando apropriado
✓ Herança quando lógico

---

8️⃣ COMPARAÇÃO COM CONCEITOS
============================

                Sobrescrita    Duck Typing
Herança         Sim            Não
Complexidade    Média          Baixa
Flexibilidade   Boa            Máxima
Tipo            Importa        Não importa

---

9️⃣ VERIFICAR MÉTODO ANTES DE USAR
==================================

def realizar_pagamento(metodo, valor):
    # Verificar se tem o método
    if hasattr(metodo, 'processar'):
        print(metodo.processar(valor))
    else:
        print("Método inválido!")

---

🔟 RESUMO RÁPIDO
=================

SOBRESCRITA:
  class Animal:
      def som(self):
          pass
  
  class Cachorro(Animal):
      def som(self):  # Sobrescreve
          return "Au au!"

DUCK TYPING:
  # Sem herança
  class A:
      def metodo(self):
          pass
  
  class B:
      def metodo(self):
          pass

POLIMORFISMO = Flexibilidade máxima!

==========================================
Última atualização: 2026-06-16
Nível: Intermediário
==========================================
